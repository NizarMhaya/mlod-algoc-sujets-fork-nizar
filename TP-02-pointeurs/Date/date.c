#include <stdio.h>
#include "Date.h"


void afficheDate(Date *date) {
    printf("Date : %d %d %d\n", date->jour, date->mois, date->annee);
}

// Fonction pour créer une date par copie
void initialiseDate(Date * d) {
    //struct Date nouvelleDate;
    nouvelleDate.jour = 1;
    nouvelleDate.mois = JANVIER;
    nouvelleDate.annee = 2000;
    return nouvelleDate;
}
